# Copy to site.env and customize for your derived repository.
# Do not commit real site.env values.

########################################
# Required                              #
########################################

# Short identifier used for compose service/router names.
SITE_NAME=docs

# One or more hostnames (space-separated).
SITE_HOSTS="docs.example.com www.docs.example.com"

# Image reference produced by CI (GHCR).
SITE_IMAGE=ghcr.io/example/docs:latest

########################################
# Optional (with defaults)              #
########################################

# Internal application container port.
SITE_PORT=80

# Base directory where deploy compose files are written.
TARGET_DIR=/opt/sites

# Shared Traefik network name.
NETWORK_NAME=traefik_proxy

# Traefik router entrypoints and certificate resolver.
ENTRYPOINTS=websecure
CERT_RESOLVER=cf

# Optional middleware chain (comma-separated).
MIDDLEWARES=

# Deploy immediately after compose generation.
DEPLOY_NOW=true

# Overwrite existing compose file.
FORCE=false

# Optional repository path used by cleanup.sh if you keep a second clone.
SITE_REPO_DIR=

########################################
# Optional private resume link flow     #
########################################

# Base URL used by scripts/generate_private_resume_link.sh
SITE_BASE_URL=https://docs.example.com

# HMAC secret used to sign expiring resume links.
# Generate with: openssl rand -hex 32
RESUME_SIGNING_SECRET=

# Route protected by token verification.
RESUME_ROUTE=/_private/resume

# Default TTL for generated links (seconds).
RESUME_LINK_TTL_SECONDS=900

# In-container path where the private resume markdown is expected.
RESUME_PRIVATE_FILE=/run/private/resume.md

# Host path to private resume markdown file (optional).
# If set, deploy_to_host.sh mounts it read-only into RESUME_PRIVATE_FILE.
RESUME_PRIVATE_FILE_HOST=

########################################
# Optional Ask Assistant settings         #
########################################

# Required to enable Ask Assistant API endpoint.
OPENAI_API_KEY=

# Optional OpenAI settings.
ASK_ASSISTANT_MODEL=gpt-4o-mini
ASK_ASSISTANT_MAX_TOKENS=700
ASK_ASSISTANT_TEMPERATURE=0.2
ASK_ASSISTANT_TIMEOUT_MS=30000

# Optional Ask Assistant rate limits.
ASK_ASSISTANT_RATE_LIMIT_WINDOW_SECONDS=300
ASK_ASSISTANT_RATE_LIMIT_MAX=10
ASK_ASSISTANT_MAX_QUESTION_CHARS=1200
